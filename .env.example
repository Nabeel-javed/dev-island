# Optional: dedicated fine-grained GitHub token, public repository access only.
# Keep private repository and write permissions disabled. Never prefix with NEXT_PUBLIC_.
GITHUB_TOKEN=
# Optional for local previews; required shared cache for multi-instance public deployment.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Public deployment origin used for absolute social-card URLs.
SITE_URL=http://localhost:3100
